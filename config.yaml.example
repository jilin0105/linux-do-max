# LinuxDO 签到配置文件
# 优先级：环境变量 > 此配置文件

# ========== 账号配置 ==========
# 用户名（可选，首次登录后会保存登录状态）
username: ""
# 密码（可选）
password: ""
# Cookie（备选登录方式，可选）
cookie: ""

# ========== 浏览器配置 ==========
# 用户数据目录（保存登录状态）
# 默认: ~/.linuxdo-browser/
user_data_dir: ""

# 是否无头模式（默认 false，有头模式）
# 有头模式 CF 验证通过率更高
# 无图形界面的服务器建议设为 true
headless: false

# 浏览器路径（可选，留空自动检测）
# Linux 常见路径:
#   /usr/bin/chromium-browser (Ubuntu/Debian)
#   /usr/bin/chromium (Arch/Fedora)
#   /snap/bin/chromium (Snap 安装)
#   /usr/bin/google-chrome (Chrome)
browser_path: ""

# Chrome 额外启动参数（可选）
#
# 重要说明：
# - Linux 系统会自动添加 --no-sandbox 等必要参数
# - 通常不需要手动配置此项
# - 如果浏览器仍然启动失败，可以尝试添加以下参数
#
# 常用参数:
#   --no-sandbox          Linux 必需（已自动添加）
#   --disable-dev-shm-usage  避免 /dev/shm 空间不足（已自动添加）
#   --disable-gpu         禁用 GPU 加速（已自动添加）
#   --headless=new        新版无头模式
#   --remote-debugging-port=9222  指定调试端口
#
# 示例:
#   chrome_args:
#     - "--no-sandbox"
#     - "--disable-dev-shm-usage"
#     - "--disable-gpu"
chrome_args: []

# ========== 签到配置 ==========
# 浏览帖子数量
browse_count: 10

# 点赞概率（0-1，0.3 表示 30%）
like_probability: 0.3

# 浏览间隔（秒）
browse_interval_min: 15
browse_interval_max: 30

# ========== Telegram 通知 ==========
# Bot Token（从 @BotFather 获取）
tg_bot_token: ""

# Chat ID（从 @userinfobot 获取）
tg_chat_id: ""

# ========== 缓存清理（仅 Linux）==========
# Linux 系统（容器/VPS）每次签到后强制清理浏览器缓存
# 仅保留登录状态（Cookies/Local Storage），清除所有缓存和临时文件
# 此行为不可关闭，确保低容量环境不会因缓存膨胀导致磁盘满
# Windows/macOS 不受影响，自动跳过

# Chrome 磁盘缓存大小限制（MB），仅 Linux 生效
# 容器环境默认 50MB，普通 Linux 默认 100MB
# 设为 0 使用上述默认值，设为其他正整数自定义限制
# 默认: 0（自动）
cache_size_limit: 0

# 磁盘剩余空间告警阈值（MB），仅 Linux 生效
# 签到前检测磁盘空间，低于此值时自动清理缓存再启动浏览器
# 适用于小容量 VPS、Docker 容器等环境
# 设为 0 禁用磁盘空间检查
# 默认: 500
disk_free_threshold: 500
